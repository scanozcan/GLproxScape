% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{fetch_hocomoco_pwm}
\alias{fetch_hocomoco_pwm}
\title{Fetch a HOCOMOCO PWM for a TF symbol.}
\usage{
fetch_hocomoco_pwm(tf_name, version = "v12", species = "human")
}
\arguments{
\item{tf_name}{TF gene symbol. Matched case-insensitively against the
HOCOMOCO bundle (HOCOMOCO uses uppercase symbols internally).}

\item{version}{HOCOMOCO release: \code{"v12"} (default) or \code{"v11"}.}

\item{species}{\code{"human"} (default) or \code{"mouse"}.}
}
\value{
PWM list \code{(id, name, pwm, len)}, or \code{NULL} if the TF
  is absent from the loaded HOCOMOCO bundle.
}
\description{
Drop-in replacement for \code{\link{fetch_jaspar_pwm}}: same return
shape \code{list(id, name, pwm, len)} where \code{pwm} is a 4 x L
log-odds matrix against a uniform background.
}
