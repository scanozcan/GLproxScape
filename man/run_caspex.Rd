% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{run_caspex}
\alias{run_caspex}
\title{Run the full CasPEX analysis pipeline}
\usage{
run_caspex(
  gene,
  grnas,
  data_files,
  tf_universe = NULL,
  epi_universe = NULL,
  out_dir = "caspex_output",
  species = "homo_sapiens",
  transcript = "canonical",
  upstream = 2500,
  downstream = 500,
  pval_thresh = 0.05,
  min_regions = 2,
  min_lfc = 0,
  top_n = 25,
  motif_tfs = NULL,
  n_common = 20,
  n_shared = 20,
  n_specific = 20,
  motif_thresh = 0.8,
  motif_score_weight = c("none", "linear", "log"),
  tfs_only = TRUE,
  kernel_sigma = 300,
  min_weight_frac = 0.15,
  min_peak_dist = 150,
  merge_dist = 100,
  coverage_correct = TRUE,
  cov_floor = 0.05,
  edge_guard_frac = 0.25,
  zone_peak_frac = 0.5,
  max_events_per_tf = 30,
  merge_position = c("argmax", "centroid"),
  max_grna_distance = NULL,
  edge_grna_weight_cap = NULL,
  position_stability = c("none", "wild_bootstrap"),
  n_bootstrap = 200L,
  weight_mode = "z",
  signal_weight = NULL,
  chipatlas = FALSE,
  chipatlas_threshold = "05",
  chipatlas_max_experiments = 100,
  special_interest_gene = NULL,
  special_interest_cap = NULL,
  chipatlas_quiet = TRUE,
  detail_top_n = 12,
  deconv_min_motif_hits = 0,
  deconv_min_max_lfc = 0,
  motif_scan_pool = c("selected", "spatial_all"),
  save_plots = TRUE,
  plot_width = 10,
  plot_height = 8
)
}
\arguments{
\item{gene}{HGNC gene symbol (character)}

\item{grnas}{Named character vector: names = region IDs matching data_files,
values = protospacer sequences (17-23bp, PAM optional).
Set to NA for regions without a matched gRNA sequence.}

\item{data_files}{Named character vector: names = region IDs, values = file paths}

\item{tf_universe}{(see function body).}

\item{epi_universe}{(see function body).}

\item{out_dir}{Output directory (created if missing)}

\item{species}{Ensembl species string (default "homo_sapiens")}

\item{transcript}{(see function body).}

\item{upstream}{bp upstream of TSS to fetch/plot (default 2500)}

\item{downstream}{bp downstream of TSS to fetch/plot (default 500)}

\item{pval_thresh}{p-value significance cutoff (default 0.05)}

\item{min_regions}{Keep TFs detected in at least this many regions (default 2)}

\item{min_lfc}{(see function body).}

\item{top_n}{Number of top TFs to show in spatial track (default 25)}

\item{motif_tfs}{Character vector of TF names to run JASPAR motif scan on.
If NULL, uses top 10 TFs from spatial model.}

\item{n_common}{(see function body).}

\item{n_shared}{(see function body).}

\item{n_specific}{(see function body).}

\item{motif_thresh}{JASPAR PWM score threshold as fraction of max (default 0.80)}

\item{motif_score_weight}{(see function body).}

\item{tfs_only}{Restrict spatial model to TFDatabase=="exist" (default TRUE)
β(x) = s(x) / max(C(x), cov_floor · max(C)) — i.e. divide the smoothed
enrichment signal by the labeling-opportunity map to recover occupancy
in gRNA-poor gaps. Each motif is evaluated independently, so every
candidate with local signal support is surfaced (no NNLS sparsity
pressure). Default FALSE; the default keeps the smoothed-s(x) NNLS
production path.}

\item{kernel_sigma}{(see function body).}

\item{min_weight_frac}{(see function body).}

\item{min_peak_dist}{(see function body).}

\item{merge_dist}{(see function body).}

\item{coverage_correct}{(see function body).}

\item{cov_floor}{Relative floor
on the denominator: the effective amplification is capped at about
1/cov_floor. Default 0.05 (≈ 20× cap). Lower values amplify distal
signals more aggressively and also amplify noise.}

\item{edge_guard_frac}{(see function body).}

\item{zone_peak_frac}{(see function body).}

\item{max_events_per_tf}{(see function body).}

\item{merge_position}{(see function body).}

\item{max_grna_distance}{(see function body).}

\item{edge_grna_weight_cap}{(see function body).}

\item{position_stability}{(see function body).}

\item{n_bootstrap}{(see function body).}

\item{weight_mode}{(see function body).}

\item{signal_weight}{(see function body).}

\item{chipatlas}{(see function body).}

\item{chipatlas_threshold}{(see function body).}

\item{chipatlas_max_experiments}{(see function body).}

\item{special_interest_gene}{(see function body).}

\item{special_interest_cap}{(see function body).}

\item{chipatlas_quiet}{(see function body).}

\item{detail_top_n}{(see function body).}

\item{deconv_min_motif_hits}{(see function body).}

\item{deconv_min_max_lfc}{(see function body).}

\item{motif_scan_pool}{(see function body).}

\item{save_plots}{Save plots to out_dir as PDF (default TRUE)}

\item{plot_width}{PDF width in inches (default 12)}

\item{plot_height}{PDF height in inches (default 8)}
}
\value{
Invisibly, a list with: spatial_df, long_data, pos_map,
        gene_info, promoter_info, motif_results, binding_events, plots
}
\description{
Run the full CasPEX analysis pipeline
}
