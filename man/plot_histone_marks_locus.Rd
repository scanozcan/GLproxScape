% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_epigenetic.R
\name{plot_histone_marks_locus}
\alias{plot_histone_marks_locus}
\title{Locus-level histone-marks summary page (one PDF page).}
\usage{
plot_histone_marks_locus(
  histone_data,
  pos_map,
  gene_info,
  upstream = 2500,
  downstream = 500
)
}
\arguments{
\item{histone_data}{list returned by fetch_histone_peaks_for_locus().}

\item{pos_map}{passed-through gRNA position vector (for the gRNA
ticks at the top).}

\item{gene_info}{passed-through gene info (for plot title).}

\item{upstream}{bp upstream of TSS for the x-axis.}

\item{downstream}{bp downstream of TSS for the x-axis.}
}
\description{
Two stacked sections of six rows each. Top section: cell-type-matched
(e.g. HEK293T) consensus peaks per mark. Bottom section: all-cell-types
aggregated consensus peaks per mark. Same x-axis as the per-factor pages
(bp TSS-relative, gRNA positions and TSS dashed line for context).
}
\details{
Active marks (warm fill) on top of each section, then repressive marks
(cool fill). Empty rows still render with their label so absence-of-data
is visually distinct from absence-of-signal — readers see "this mark
wasn't measured in HEK293T at this locus" rather than the row vanishing.
}
