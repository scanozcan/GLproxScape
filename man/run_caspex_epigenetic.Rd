% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_epigenetic.R
\name{run_caspex_epigenetic}
\alias{run_caspex_epigenetic}
\title{Run the epigenetic-factor binding-zone analysis on a CasPEX result.}
\usage{
run_caspex_epigenetic(
  result,
  epigenetic_factors,
  zone_frac = 0.3,
  inner_zone_frac = 0.7,
  centroid_frac = 0.7,
  kernel_sigma = NULL,
  weight_mode = NULL,
  cov_floor = 0.05,
  edge_guard_frac = 0.25,
  edge_grna_weight_cap = NULL,
  max_grna_distance = NULL,
  chipatlas = TRUE,
  chipatlas_threshold = "05",
  chipatlas_max_experiments = 100,
  special_interest_gene = NULL,
  special_interest_cap = NULL,
  chipatlas_quiet = TRUE,
  upstream = 2500,
  downstream = 500,
  detail_top_n = 50,
  min_n_regions = 2,
  subtract_tf_overlap = FALSE,
  peak_signal_range = NULL,
  histone_marks_pdf = TRUE,
  histone_marks = c("H3K4me3", "H3K27ac", "H3K4me1", "H3K36me3", "H3K27me3", "H3K9me3"),
  histone_cell_type = "HEK293T",
  histone_max_experiments_matched = 50,
  histone_max_experiments_all = 50,
  epigenetic_complexes_csv = NULL,
  epigenes_main_csv = NULL,
  complex_min_detected = 2,
  complex_max_pages = 50,
  out_dir,
  plot_width = 12,
  plot_height = 8,
  save_plots = TRUE
)
}
\arguments{
\item{result}{output of run_caspex().}

\item{epigenetic_factors}{character vector of HGNC symbols (e.g. read
from EpiDatabase.txt).}

\item{zone_frac}{β / max(β) threshold for zone detection
(default 0.3; lower than TF path's 0.5 to
merge near-peak β values into broader
domains under the same labelling kernel).}

\item{inner_zone_frac}{(see function body).}

\item{centroid_frac}{(see function body).}

\item{kernel_sigma}{labelling-radius σ in bp; defaults to
result$kernel_sigma (typically 300).}

\item{weight_mode}{region-weight mode; defaults to
result$weight_mode.}

\item{cov_floor}{(see function body).}

\item{edge_guard_frac}{(see function body).}

\item{edge_grna_weight_cap}{(see function body).}

\item{max_grna_distance}{(see function body).}

\item{chipatlas}{fetch + render ChIP-Atlas peaks (default TRUE).}

\item{chipatlas_threshold}{(see function body).}

\item{chipatlas_max_experiments}{(see function body).}

\item{special_interest_gene}{(see function body).}

\item{special_interest_cap}{(see function body).}

\item{chipatlas_quiet}{(see function body).}

\item{upstream}{(see function body).}

\item{downstream}{(see function body).}

\item{detail_top_n}{max number of factor pages in the deck
(default 50; ranked by max peak β).}

\item{min_n_regions}{passed to run_spatial_model.}

\item{subtract_tf_overlap}{if TRUE, drop epigenetic factors that are
also in result$motif_results (i.e. were
motif-scanned in the TF deck) so the
epigenetic deck does not duplicate them.
Default FALSE — render both decks for dual-
class proteins (GATA, FOX, KLF, KRAB-ZNFs)
so the reader can compare bubble vs.
zone framings.}

\item{peak_signal_range}{(see function body).}

\item{histone_marks_pdf}{(see function body).}

\item{histone_marks}{(see function body).}

\item{histone_cell_type}{(see function body).}

\item{histone_max_experiments_matched}{(see function body).}

\item{histone_max_experiments_all}{(see function body).}

\item{epigenetic_complexes_csv}{(see function body).}

\item{epigenes_main_csv}{(see function body).}

\item{complex_min_detected}{(see function body).}

\item{complex_max_pages}{(see function body).}

\item{out_dir}{output directory; created if absent.}

\item{plot_width}{(see function body).}

\item{plot_height}{(see function body).}

\item{save_plots}{(see function body).}
}
\value{
invisible list with components: spatial_df_epi, zones_df,
  chipatlas_peaks, factors_present, factors_missing, kernel_sigma,
  zone_frac, weight_mode.
}
\description{
Designed to be called AFTER `run_caspex()` on the same locus, taking the
returned result list as input. Reuses `result$long_data`, `result$pos_map`,
`result$gene_info`, `result$promoter_info`, `result$kernel_sigma`,
`result$weight_mode` — no inputs are reloaded from disk. Mirrors the
`run_caspex_extras()` ergonomic pattern.
}
\details{
What this function does, in order:
  1. Filter result$long_data to the supplied epigenetic factor list.
     Reports the detection-overlap rate (factors in list ∩ proteins
     detected in long_data) — the only proteins we can model.
  2. Run the same spatial model (run_spatial_model) used by run_caspex(),
     but with tfs_only = FALSE so the TFDatabase filter doesn't drop
     epigenetic factors that happen to be missing the isTF flag.
  3. Per surviving factor, call predict_binding_zones_epigenetic() to
     get its zone bars.
  4. Optionally fetch ChIP-Atlas peaks for each factor in the deck
     (default ON; same wrapper as the TF path).
  5. Compute per-zone ChIP-Atlas overlap statistics
     (compute_chipatlas_overlap).
  6. Generate a per-factor PDF deck and write zones / spatial CSVs.
}
