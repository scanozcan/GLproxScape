% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_extras.R
\name{plot_tf_cooccurrence}
\alias{plot_tf_cooccurrence}
\title{Heat-map of TF-pair co-occurrence of predicted binding events.}
\usage{
plot_tf_cooccurrence(
  result,
  tol_bp = 50,
  min_events = 1,
  top_peak_tfs = Inf,
  top_tfs = 50
)
}
\arguments{
\item{result}{The list returned by run_caspex() (long_data, pos_map, spatial_df, binding_events, motif_results, ...).}

\item{tol_bp}{Tolerance in bp for matching a predicted event to a ChIP-Atlas peak (default 25).}

\item{min_events}{Minimum number of events required to include a transcription factor.}

\item{top_peak_tfs}{Number of top motif-free peak-predicted transcription factors to display.}

\item{top_tfs}{Number of top transcription factors to display.}
}
\value{
A \code{ggplot} object.
}
\description{
For every pair of TFs with called events, count how many events of one
TF fall within `tol_bp` of an event of the other TF. The resulting
symmetric matrix (proportion-scaled) is a candidate-cofactor screen:
pairs lighting up have overlapping binding footprints on this promoter.
}
\details{
Mode-agnostic: works identically for default and coverage-aware events
because it only consumes the `tf` / `position` columns of
`result$binding_events`.
}
