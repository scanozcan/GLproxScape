% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{plot_tf_track}
\alias{plot_tf_track}
\title{Generic genome track for a subset of TFs, with optional JASPAR overlay}
\usage{
plot_tf_track(
  tfs,
  spatial_df,
  pos_map,
  promoter_info,
  motif_results = NULL,
  lane_labels = NULL,
  upstream = 2500,
  downstream = 500,
  title = "Predicted TF binding zones",
  subtitle = NULL,
  chipatlas_peaks = NULL
)
}
\arguments{
\item{tfs}{Character vector of TF symbols to plot (in order).}

\item{spatial_df}{From run_spatial_model()}

\item{pos_map}{From match_all_grnas()}

\item{promoter_info}{From fetch_promoter_seq()}

\item{motif_results}{From run_motif_scan(), or NULL to hide motif ticks}

\item{lane_labels}{Optional named char vec (names = TF symbol, values =
label to show on the left). Defaults to TF symbol.}

\item{upstream}{bp upstream of TSS}

\item{downstream}{bp downstream of TSS}

\item{title, subtitle}{Plot text (subtitle auto-generated if NULL)}

\item{chipatlas_peaks}{ChIP-Atlas peak table for the TF (srx, cell_type, start_rel, end_rel); NULL skips the overlay.}
}
\value{
ggplot, or NULL if no TFs to plot
}
\description{
One lane per TF, stacked vertically. Each lane shows:
  * Gaussian enrichment ribbon centred on the spatial centroid
  * Lane-local centroid tick
  * Optional motif hit ticks inside the lane (only if motif_results provided)
Global reference lines (TSS dashed; sgRNA dotted) span the full plot.
}
