% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_epigenetic.R
\name{plot_epigenetic_zone_deck}
\alias{plot_epigenetic_zone_deck}
\title{Per-factor epigenetic zone deck page (Plot 10 analogue).}
\usage{
plot_epigenetic_zone_deck(
  tf_name,
  long_data,
  pos_map,
  zones_df,
  kernel_sigma = 300,
  upstream = 2500,
  downstream = 500,
  weight_mode = "lfc_signed",
  zone_frac = 0.3,
  inner_zone_frac = 0.7,
  cov_floor = 0.05,
  edge_guard_frac = 0.25,
  peak_signal_range = NULL,
  chipatlas_peaks = NULL
)
}
\arguments{
\item{tf_name}{(see function body).}

\item{long_data}{(see function body).}

\item{pos_map}{(see function body).}

\item{zones_df}{(see function body).}

\item{kernel_sigma}{(see function body).}

\item{upstream}{(see function body).}

\item{downstream}{(see function body).}

\item{weight_mode}{(see function body).}

\item{zone_frac}{(see function body).}

\item{inner_zone_frac}{(see function body).}

\item{cov_floor}{(see function body).}

\item{edge_guard_frac}{(see function body).}

\item{peak_signal_range}{(see function body).}

\item{chipatlas_peaks}{(see function body).}
}
\description{
Renders one page for a single epigenetic factor with three lanes:
  1. Upper panel: CasPEX signal curve s(x) + per-region logFC stems.
  2. Middle lane: zone bars (horizontal coloured rectangles spanning
     [zone_start, zone_end]) with a diamond at peak_position. Replaces
     the bubble + motif-tick lane from the TF deck.
  3. Lower lane: ChIP-Atlas SRX rows (when peaks are passed in).
}
\details{
No motif tick lane (epigenetic factors typically have none); no MLE
position track (MLE is a TF-specific diagnostic that disambiguates the
"low logFC could be far binder" case for sequence-specific anchors).
}
