% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{download_hocomoco_bundle}
\alias{download_hocomoco_bundle}
\title{Download a HOCOMOCO MEME bundle to the local cache.}
\usage{
download_hocomoco_bundle(
  version = "v12",
  species = "human",
  force = FALSE,
  from_file = NULL
)
}
\arguments{
\item{version}{HOCOMOCO release: \code{"v12"} (default) or \code{"v11"}.}

\item{species}{\code{"human"} (default) or \code{"mouse"}.}

\item{force}{Logical. Redownload even if the cache file already
exists. Default \code{FALSE}.}

\item{from_file}{Optional path to a manually-downloaded MEME file. If
provided, the file is copied into the cache slot for
\code{(version, species)} instead of being fetched from
the network. Use this if the automatic download is
blocked.}
}
\value{
The path to the cached bundle (invisibly).
}
\description{
Lazy-loaded on first call to \code{\link{fetch_hocomoco_pwm}} or
\code{\link{load_hocomoco_pwms}}; explicit invocation is rarely
necessary outside firewalled environments where the automatic
download is blocked.
}
\examples{
\dontrun{
download_hocomoco_bundle(version = "v12", species = "human")
}
}
