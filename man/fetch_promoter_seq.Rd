% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{fetch_promoter_seq}
\alias{fetch_promoter_seq}
\title{Fetch promoter sequence from Ensembl (strand-aware, TSS-centred)}
\usage{
fetch_promoter_seq(
  gene_info,
  upstream = 2500,
  downstream = 500,
  species = NULL
)
}
\arguments{
\item{gene_info}{Output of lookup_gene()}

\item{upstream}{bp upstream of TSS to include}

\item{downstream}{bp downstream of TSS to include}

\item{species}{Ensembl species token for the `/sequence/region/<species>`
endpoint. Defaults to `gene_info$species` (set by
lookup_gene()), falling back to "homo_sapiens" for
backward compatibility with any hand-built gene_info
lists that pre-date the species field. Previously this
was hardcoded to "human", which silently routed every
non-human gene to the human genome.}
}
\value{
List: seq (character), tss_offset (int), seq_start, seq_end, strand
}
\description{
Fetch promoter sequence from Ensembl (strand-aware, TSS-centred)
}
