% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caspex_analysis.R
\name{fetch_jaspar_pwm}
\alias{fetch_jaspar_pwm}
\title{Fetch position weight matrix from JASPAR for a TF name}
\usage{
fetch_jaspar_pwm(tf_name, host = "https://jaspar.elixir.no")
}
\arguments{
\item{tf_name}{HGNC symbol}

\item{host}{JASPAR API host (default current canonical mirror)}
}
\value{
List with id, name, pwm (4×L matrix, rows=ACGT), length; or NULL
}
\description{
JASPAR REST API uses `tf_name` (case-sensitive, exact) or `search`
(substring) as query parameters. `version` is not a valid filter — the
latest-version profile is selected by ordering via `-version`.
We try `tf_name` first, then fall back to `search` if empty.
}
\details{
Results are cached in `.caspex_pwm_cache` keyed by (TF, host) so
repeated calls within one R session return the identical PWM. This
keeps A/B runs (default vs coverage-aware) reproducible.
}
\examples{
\dontrun{
pwm <- fetch_jaspar_pwm("CTCF")
}
}
